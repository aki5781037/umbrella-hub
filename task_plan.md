# 目标
修复客户管理中新建客户保存后跳转详情页返回 404 的问题。

# 范围
只处理客户新建、保存重定向、客户详情页读取相关代码。

# 非目标
不重构整体数据存储、不调整客户表单文案和样式。

# 阶段
1. 检查客户表单、保存接口、详情页和数据读写。
2. 做最小修复。
3. 运行构建或 HTTP 流程验证。

# 验收标准
新建客户 POST 成功后，重定向到客户详情页并返回 200，不再显示 404。
