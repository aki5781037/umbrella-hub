# 目标
把外贸业务驾驶舱重构成可上线使用的客户、项目、生产一体化管理系统，并在本地完成全站测试和问题修复。部署到网站作为最后阶段执行，执行前需要用户确认远程写入授权和部署目标。

# 范围
处理项目管理、生产管理、客户门户、全站导航、主要按钮流程、构建验证、浏览器验证和部署前检查。

# 非目标
不接入复杂 ERP、仓库系统、货代系统或工厂系统；不在未确认前执行生产部署、远程写入或使用真实凭据。

# 阶段
1. 新建工作分支并检查当前项目结构。
2. 重构项目管理页为客户列表、项目看板、项目详情三栏。
3. 重构新建项目页为外贸项目模板选择入口。
4. 增加生产管理模块入口和从接单到出货的生产流程页面。
5. 让客户门户按客户展示自己的项目和生产订单。
6. 全站测试导航、表单、按钮、详情、返回、登录和退出。
7. 修复测试发现的问题。
8. 准备部署清单，确认授权后部署到网站并做线上验证。已完成。
9. 增加系统设置里的数据备份策略、自动备份、手动备份和恢复备份。已部署到线上并验证。

# 备份策略
- 备份范围：`crm-records.json`、`portal-submissions.json`、`emails.json`、`mail-config.json`。
- 备份位置：服务器持久化目录 `data/backups`，线上对应 Docker volume `/app/data/backups`。
- 自动备份：管理员可在“系统设置”里设置启用状态、间隔小时数和最多保留备份数量。
- 手动备份：管理员可随时点击“立即手动备份”生成当前快照。
- 恢复备份：恢复前系统先生成 `pre-restore` 备份，再覆盖当前目标文件。
- 风险提示：如已配置真实邮箱，备份会包含邮箱配置文件，应限制服务器访问权限并定期做外部离线备份。

# 部署前清单
- 目标站点：需要用户提供域名、服务器或托管平台。
- 部署方式：需要确认是 Vercel、宝塔/Node 服务器、Docker、PM2 还是其他方式。
- 环境变量：需要确认 `ADMIN_EMAIL`、`ADMIN_PASSWORD`、`PORTAL_EMAIL`、`PORTAL_PASSWORD`、`PORTAL_CUSTOMER_ID`。
- 邮件配置：如启用真实 IMAP，需要确认邮箱服务商、IMAP host、port、SSL、账号和授权码。
- 数据持久化：需要确认 `data/crm-records.json`、`data/portal-submissions.json`、邮件配置和邮件数据是否放在服务器持久目录、数据库或备份策略内。
- 线上验证：部署后检查登录、导航、客户、项目、生产、客户门户、表单提交、任务处理和移动端展示。

# 验收标准
网站本地和线上均可访问；管理员能管理客户、项目和生产订单；客户门户能看到自己的项目和生产订单；所有主要按钮和页面流程测试通过；发现的问题已修复或明确记录。

## 2026-05-23 备份下载补充
- 备份策略已增加离线下载：管理员可在 `https://crm.arkumbrella.com/settings` 下载单个备份 JSON 文件。
- 下载接口为 `/api/backups/[id]`，仅登录管理员可访问，并以附件形式返回备份快照。
- 下载文件可能包含 `mail-config.json` 等配置数据；保存到本地后应按敏感资料管理。

## 2026-05-23 客户门户提交项目
- 目标：客户在门户提交新项目需求后，后台自动生成项目卡并进入项目管理流程。
- 范围：`/portal` 表单、`/api/portal/projects` 接口、项目数据写入、后台项目详情阶段展示和门户 API 权限边界。
- 完成标准：客户提交后能看到新项目详情；管理员在项目看板和客户详情中能看到同一项目；客户不能操作其他客户项目。

## 2026-05-23 项目看板细化目标
- 目标：让项目管理页直接作为三栏工作台使用，减少跳转和空内容。
- 范围：移除顶部统计栏、同页选择项目、隐藏空项目分组、在详情栏上传附件、结束项目后隐藏。
- 验证：本地构建、后台登录页面检查、附件上传流程、结束项目流程和测试数据清理。

## 2026-05-23 项目详情交互目标
- 目标：让右侧项目详情面板承担日常操作，而不是只展示静态信息。
- 范围：中间栏单列项目卡、右侧详情放大、流程节点推进、子任务勾选、附件下载和结束项目入口外显。
- 完成标准：管理员能在三栏页内选择项目、推进流程、勾选任务、上传并下载附件、结束项目后看板隐藏。

## 2026-05-23 生产管理交互目标
- 目标：把生产管理页改成可操作的订单管理台，订单列表选择后在右侧展示详情。
- 范围：`/production` 页面、生产订单数据层、生产订单流程推进、附件上传、结束订单、客户门户生产订单展示。
- 非目标：不接入真实 ERP、工厂系统、仓库系统或物流系统。
- 完成标准：管理员能选择订单、推进生产流程、上传资料附件、结束订单；客户门户只能看到自己订单的生产进度。
- 回归补充：生产子任务需要支持完成 / 重新打开 / 删除，生产订单 COMMENTS 需要支持内部和客户双向同步。

## 2026-05-23 项目详情任务与 COMMENTS
- 目标：让项目详情里的子任务和 COMMENTS 可以直接维护，并让客户门户留言同步显示到后台详情。
- 范围：`/projects` 三栏详情、`/projects/[id]` 详情页、项目任务 API、内部 COMMENTS API、客户门户项目详情。
- 非目标：不做富文本评论、评论附件、通知推送或权限分级审批。
- 完成标准：后台详情不再显示“项目字段”；管理员能新增、勾选、删除子任务并添加 COMMENTS；客户能在门户添加 COMMENTS；双方 COMMENTS 在后台和门户详情中都能看到。

## 2026-05-23 生产管理三栏与协作目标
- 目标：生产管理页改为客户列表、客户订单列表、订单详情三栏。
- 范围：`/production` 三栏选择、生产订单子任务增删勾选、订单 COMMENTS、客户门户生产订单详情访问与客户评论。
- 非目标：不开放客户修改后台生产流程、结束订单、附件上传或子任务管理。
- 完成标准：管理员按客户筛选订单并操作详情；客户能进入自己订单详情并添加评论，不能访问其他客户订单。

## 2026-05-23 生产文档识别目标
- 目标：生产步骤上传合同或生产单后，自动识别 Excel 内容并写回订单信息与生产资料。
- 范围：`/production` 节点上传表单、生产附件 API、Excel 解析、订单明细字段、生产单资料字段、后台和客户门户 COMMENTS 样式。
- 非目标：不接入 OCR、PDF 表格解析、ERP 或真实工厂系统；非 `.xlsx` 文件仅作为普通附件或文本尝试解析。
- 完成标准：合同上传可填入客户名字、订单号、数量、金额、交货日期；生产单上传可填入伞面、伞骨、包装、布标、吊牌、外箱等资料；客户留言醒目显示。
# 2026-05-23 客户账号管理补充
- 目标：管理员能在系统设置中为客户创建/更新门户账号，并绑定到具体客户。
- 范围：账号持久化、登录识别、设置页账号表单、客户门户项目和生产订单隔离验证。
- 非目标：不接入短信/邮箱验证码、SSO、数据库用户表或密码哈希迁移。
- 完成标准：新客户账号登录后只看到绑定客户自己的项目和生产订单，访问其他客户项目/订单返回 404。

# 2026-05-25 项目审阅与生产订单新建修复
- 目标：审阅当前项目并修复影响生产管理闭环的明确 BUG。
- 范围：生产订单创建入口、创建 API、后台生产页、客户门户订单展示和权限隔离。
- 非目标：不部署线上、不改生产环境、不接入真实 ERP 或外部系统。
- 完成标准：管理员能从 `/production` 进入 `/production/new` 新建订单；订单能同步显示在后台三栏和客户门户；客户不能访问其他客户订单；构建通过且测试数据清理干净。
