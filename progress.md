# 已完成
- 已从 `master` 新建并切换到 `codex/project-kanban-redesign` 分支。
- 已检查项目管理页、新建项目页、数据层和 Shell 布局。
- 已生成三栏工作台和新建项目弹窗的静态概念图用于对照。
- 已将 `/projects` 改为客户列表、项目看板、项目详情三栏工作台。
- 已将 `/projects/new` 改为 12 个外贸项目模板选择入口，并支持预览精简流程和默认子任务。
- 已让新建项目提交时把模板子任务保存进项目任务列表。
- 已完成构建、桌面截图和移动端无横向溢出检查。
- 已在左侧导航增加“生产管理”，并新增 `/production` 生产订单流程页。
- 已验证 `/production` 桌面页面，左侧导航高亮、生产订单、生产流程看板和订单详情均正常展示。
- 已确认 390px 移动端 `/production` 无横向溢出。
- 已把整体目标更新为客户、项目、生产、客户门户、全站测试和部署前检查。
- 已抽出 `lib/production.ts` 生产订单数据层，并在客户详情、客户门户和生产管理页复用。
- 已让客户门户按登录客户过滤项目和生产订单，并阻止客户访问其他客户项目详情。
- 已完成最终构建验证：`npm.cmd run build` 通过，仅保留 `app/umbrella-catalog/page.tsx` 既有 `<img>` lint 警告。
- 已完成最终浏览器烟测：后台登录、客户登录、主导航、客户新建、项目模板新建、邮件设置弹窗、伞款搜索清空、客户门户提交、任务处理按钮均通过。
- 已补充验证邮件中心“模拟拉取”按钮；真实 IMAP 配置和同步需要邮箱凭据，未擅自执行。
- 已完成移动端 390px 检查：`/projects`、`/production`、`/customers/abc-malaysia`、`/portal` 均可正常展示。
- 已清理本轮烟测生成的 `AUTOTEST_FINAL` 临时客户、项目和门户提交数据。
- 已按用户授权执行 `scripts/deploy-fnos.ps1`，部署到 `https://crm.arkumbrella.com` 和 `https://portal.arkumbrella.com`。
- 已确认远端 `umbrella-trade-hub` Docker 容器启动正常，公开域名返回登录重定向，线上登录后核心页面内容可访问。
- 已新增“系统设置 / 备份与恢复设置”面板，支持自动备份间隔、最多保留数量、手动备份和恢复备份。
- 已实现 `data/backups` 文件备份策略，覆盖客户项目、门户提交、邮件和邮箱配置；恢复前会自动生成恢复前备份。
- 已验证本地设置页、保存备份间隔、手动备份和恢复确认弹窗；未执行真实恢复覆盖。
- 已再次执行 `npm.cmd run build`，新增 `/settings`、`/api/backups`、`/api/backups/config` 后构建通过。
- 已部署备份功能到线上，验证 `https://crm.arkumbrella.com/settings`、备份配置保存、线上手动备份和远端 `/app/data/backups` 写入均正常。

# 当前步骤
- 备份功能已部署到线上并完成验证。

# 下一步
- 建议后续把演示账号密码替换为正式生产密码。

# 阻塞点
- 暂无。

## 2026-05-23 备份下载更新
- 已新增线上备份下载入口：`/settings` 的每条备份记录现在可以点击“下载备份”。
- 已部署到 `https://crm.arkumbrella.com`，并验证 `/api/backups/{id}` 返回 `200`、`Content-Disposition: attachment`，下载 JSON 的 `id` 与备份列表一致。
- 原分片上传部署在第 8/32 片被 SSH 关闭；已改用小文件增量上传并完成远端 Docker 重建。

## 2026-05-23 部署脚本加固
- 已优化 `scripts/deploy-fnos.ps1`：优先使用 `scp -O` 上传完整发布包，并对远端压缩包做 SHA256 与 `gzip -t` 校验。
- 如果 `scp -O` 失败，脚本会回退到 base64 分片上传；分片大小从 512KB 降到 128KB，并增加 SSH keepalive。
- 已完成本地 PowerShell 语法解析检查和 `git diff --check`；未重新执行生产部署。

## 2026-05-23 客户门户新建项目
- 已新增客户门户“提交新项目需求”表单和 `/api/portal/projects` 接口，客户提交后会写入同一套 `data/crm-records.json` 项目数据。
- 新项目会自动绑定当前登录客户，后台项目看板、项目详情、客户详情和任务列表都能看到。
- 已收紧客户门户留言与确认接口：客户只能操作自己名下且门户可见的项目。
- 已用本地浏览器完成客户提交、跳转项目详情、后台同步、客户详情同步和跨客户越权 404 验证；测试项目已清理。
- 已执行 `npm.cmd run build`，构建通过；未部署到线上。

## 2026-05-23 项目看板精简与项目关闭
- 已移除 `/projects` 顶部统计卡，让页面直接进入客户列表、项目看板、项目详情三栏。
- 项目卡点击后改为同页 `project=` 选择，不再跳转到单独详情页；第三栏直接显示选中项目。
- 已过滤项目看板中的空栏目，不再显示没有项目的“待处理 / 进行中 / 等待外部反馈”等分组。
- 已在项目详情第三栏和单独项目详情页加入附件上传入口；上传文件保存到 `data/project-attachments`，文件名同步进项目附件列表。
- 已新增“结束项目”按钮；结束后项目阶段变为“已结束”、门户不可见，并从项目看板、客户项目和任务列表隐藏。
- 已把 `project-attachments` 纳入备份快照范围；已完成本地构建、API 流程验证和浏览器视觉验证，未部署到线上。

## 2026-05-23 项目详情交互增强
- 已把 `/projects` 改为更窄的单列项目卡列表，右侧项目详情栏占用更多空间。
- 流程节点已改成可点击按钮，提交到 `/api/projects/[id]/stage` 后会推进当前项目阶段并写入时间线。
- 子任务清单已改成可点击勾选，提交到 `/api/projects/[id]/tasks` 后可完成或重新打开任务。
- 上传附件现在保存原始文件名和存储文件名，并通过 `/api/projects/[id]/attachments/[file]` 支持下载。
- 已刷新浏览器到 `http://localhost:3000/projects?customer=abc-malaysia&project=foldable-umbrella-malaysia` 供查看；本地 dev server 保持运行。

## 2026-05-23 生产管理交互调整
- 当前步骤：已确认 `/production` 仍是静态生产流程看板，顶部统计栏存在，流程节点和订单卡没有可提交操作。
- 下一步：把 `lib/production.ts` 改为可持久更新的数据层，并新增生产订单的阶段推进、附件上传和关闭接口。
- 阻塞点：暂无。

## 2026-05-23 生产管理按钮回归
- 已补齐生产子任务的“完成 / 重新打开”操作，`/api/production/orders/[id]/tasks` 支持 `intent=toggle`。
- 已把 `/production` 的生产子任务卡改为可点击勾选，并保留“完成 / 重新打开 / 删除”按钮。
- 已用 `po-abc-260518` 做模拟回归：阶段推进、任务新增、任务完成、任务重新打开、COMMENTS、客户 COMMENTS、附件上传下载、越权拦截均通过。
- 已恢复 `data/production-records.json` 测试前内容，并确认无 `SIMTEST` 残留。

## 2026-05-23 项目详情任务与 COMMENTS
- 已移除 `/projects` 右侧详情里的“项目字段”区块。
- 已把子任务清单改为可新增自定义任务、勾选完成/重新打开、删除任务；默认任务也可删除。
- 已新增 `/api/projects/[id]/comments`，管理员可在项目详情添加内部 COMMENTS。
- 已把后台项目详情改为合并客户门户提交记录，客户门户新增的 COMMENTS 会同步显示在后台三栏详情里。
- 已把客户门户项目详情的 `Messages` 区块改为 `COMMENTS`，客户仍通过原安全接口提交，且只能操作自己客户名下项目。
- 已完成构建、HTTP/API 流程验证、临时数据清理和浏览器页面检查；本地预览保持在 `http://localhost:3000/projects?customer=abc-malaysia&project=foldable-umbrella-malaysia`。
- 已用模拟打样项目完成回归测试：后台任务新增/删除/勾选、后台 COMMENTS、客户 COMMENTS、越权拦截、结束项目隐藏均通过；模拟数据已清理。
- 已完成：`/production` 已改成订单列表 + 右侧订单详情；顶部统计卡已移除，原“生产流程看板”分组已移除。
- 已完成：生产订单支持写入 `data/production-records.json`，并新增阶段推进、附件上传/下载和结束订单接口。
- 已完成：客户门户“我的订单生产管理”读取同一份生产订单状态，并只显示当前登录客户自己的订单。
- 验证：`npm.cmd run build` 通过；浏览器验证订单选择、流程推进和客户门户订单隔离通过。

## 2026-05-23 生产三栏、子任务和评论
- 当前步骤：已确认生产页仍缺客户列表栏，子任务来自静态 milestones，没有增删与勾选接口，COMMENTS 尚未接入生产订单。
- 下一步：扩展 `lib/production.ts` 任务和评论数据模型，新增生产订单 tasks/comments 接口。
- 阻塞点：暂无。
- 已完成：`/production` 已改为客户列表、订单列表、订单详情三栏，URL 保留 `customer` 和 `order` 选择状态。
- 已完成：生产子任务支持勾选/重新打开、自定义新增和删除；COMMENTS 支持后台和客户添加。
- 已完成：客户门户订单卡新增“查看生产订单”，并新增 `/portal/production/[id]` 详情页，客户只能访问自己订单。
- 验证：`npm.cmd run build` 通过；生产任务新增/勾选/删除 API、客户评论 API、客户跨订单 404、Edge 截图检查均通过。

## 2026-05-23 生产文档识别与 COMMENTS
- 已完成：每个生产流程节点增加独立上传入口，`定金/合同` 上传合同，`产前准备 / 物料采购 / 生产中` 上传生产单，其它节点上传生产资料。
- 已完成：新增 `lib/production-document-parser.ts`，合同 `.xlsx` 可识别客户名字、订单号、订单数量、订单金额、交货日期并写回订单明细。
- 已完成：生产单 `.xlsx` 可识别伞面材质、伞骨资料、包装资料、布标、吊牌、外箱、手柄和印刷，并在后台与客户门户展示。
- 已完成：生产子任务在后台改为状态展示和删除，不再提供勾选按钮；客户留言在后台和客户门户使用醒目的蓝色框。
- 已完成：为已有 `playwright.config.ts` 补回 `@playwright/test` devDependency，避免构建类型检查因缺少模块失败。
- 验证：`npm.cmd run build` 通过；合同/生产单上传 API 返回 `303` 并正确写入测试字段；测试数据和测试附件已清理；`http://localhost:3101/production?customer=korea-brand&order=po-korea-260512` 已在 in-app browser 打开且不是 404。
# 2026-05-23 客户账号管理
- 已确认当前门户项目列表、项目详情、生产订单列表和生产订单详情已有 customerId 隔离基础。
- 已新增 `data/customer-accounts.json` 持久化客户账号、`/api/customer-accounts` 保存接口和设置页“客户门户账号”表单。
- 已让登录识别同时支持固定演示账号和设置页新增的客户账号。
- 已把 `customer-accounts.json` 纳入备份范围。
- 已完成构建、HTTP 登录隔离验证和浏览器设置页验证。
- 下一步：如需上线，部署前为真实客户创建正式邮箱/密码，并替换演示账号密码。
# 2026-05-23 手机端排版与按钮测试
- 已用 390px 手机视口巡检后台首页、邮件、客户、项目、新建项目、生产、设置、客户门户项目和客户门户生产订单详情。
- 已修复 `/settings` 在手机端由备份面板和备份目标卡片撑出的横向滚动。
- 已放大客户列表/返回链接/门户返回链接/生产任务删除按钮/备份开关的触控区域。
- 已验证手机端关键点击：移动导航到客户管理、设置页刷新、客户门户项目详情、客户门户生产订单详情。
- 已执行 `npm.cmd run build` 通过，并在构建后重启本地 dev server 复测 `/settings` 无横向溢出。
- 已清理本轮测试误触产生的 `po-abc-260518` 生产订单临时更新。

# 2026-05-25 项目审阅与 BUG 修复
- 已修复生产管理缺少新建订单入口的问题：新增 `/production/new` 页面，并在 `/production` 订单列表头部加入“新建订单”按钮。
- 已复用现有 `/api/production/orders` 创建接口，表单支持客户、订单号、产品、负责人、数量、金额、工厂、优先级、风险、计划日期、出货日期、下一步动作和默认生产子任务。
- 已用 `SIMTEST-PO-*` 模拟订单验证新建、后台详情、阶段推进、任务完成、内部 COMMENTS、客户 COMMENTS、客户门户显示和跨客户 404 隔离。
- 已清理本轮 `SIMTEST/AUTOTEST` 模拟数据，`data/` 中无测试残留。
- 当前步骤：本地 dev server 保持在 `http://localhost:3000`，方便继续查看。
- 阻塞点：暂无；未执行线上部署。

# 2026-05-25 第二轮审阅与链接修复
- 已修复客户详情页“关联项目”只跳单独详情页的问题，现在直接进入三栏项目工作台并选中对应项目。
- 已修复客户详情页“生产订单”只能看不能点的问题，现在订单卡可直接进入生产管理三栏详情。
- 已修复项目详情提醒色值：绿色项目不再显示为黄色提醒。
- 已处理 `next build` 后本地 dev server 的 `.next` 缓存冲突，重启后 `/customers/abc-malaysia` 和 `/production` 恢复 200。
- 验证：`npm.cmd run build` 通过；浏览器确认客户详情显示项目与生产订单，并且两类链接均指向正确工作台。

# 2026-05-25 第三轮审阅与附件权限修复
- 已修复客户门户项目文件区只有 `Download` 文案但不能下载的问题；真实上传附件现在显示下载链接，历史记录文件显示 `Record only`。
- 已放开附件下载的客户侧权限：管理员可下载全部附件，客户只能下载自己名下项目/生产订单附件；无身份或跨客户访问返回 404。
- 已给项目附件和生产附件下载接口补充磁盘文件缺失检查，文件不存在时返回 404，不再落到 500。
- 已在客户门户生产订单详情增加“附件资料”区，客户可下载自己订单的生产资料附件。
- 验证：项目附件和生产附件均用临时数据完成上传、客户下载、无身份 404 测试；临时项目、订单、附件目录已清理。
- 移动端验证：390px 检查客户门户项目、客户门户生产订单、后台客户详情、项目工作台和生产工作台，均无横向溢出。
# 2026-05-25 第四轮关闭规则与备份恢复
- 已完成：生产订单关闭后，客户门户列表、详情页、附件下载和客户 COMMENTS 都会按关闭状态隐藏或返回 404。
- 已完成：备份恢复前会清理 `project-attachments` 和 `production-attachments` 受管附件目录，再写回备份快照，避免恢复后残留后续上传的旧文件。
- 已验证：`npm.cmd run build` 通过；仅保留 `app/umbrella-catalog/page.tsx:93` 和 `app/umbrella-catalog/page.tsx:113` 的既有 `<img>` lint 警告。
- 已验证：使用临时生产订单完成关闭回归，关闭前客户可见，关闭后客户门户列表隐藏、详情 404、客户评论 404。
- 已验证：使用隔离 `.tmp` 夹具验证备份恢复会删除快照之外的附件文件，并自动生成 1 个 restore 前备份。
- 当前步骤：本地 dev server 继续运行在 `http://localhost:3000`，方便查看 `/production?customer=abc-malaysia&order=po-abc-260518`。
- 阻塞点：暂无；未执行线上部署。
